-b команда для сборки (если нужно) (например build )
-d директория для публикации (например dist public)
-r репозиторий для публикации (GitHub repo name)
-t настройки доступа к репозиторию (токен доступа GitHub token )
